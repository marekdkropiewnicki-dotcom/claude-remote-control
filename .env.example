# Claude Remote Control - Environment Variables

# This file is just a template. Copy it to .env.local and add your actual API key
# Note: The application is designed to work without server-side API keys
# Users provide their own API keys through the web interface

# Optional: If you want to use a server-side API key instead of client-provided keys
# ANTHROPIC_API_KEY=your-api-key-here
